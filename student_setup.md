# คู่มือตั้งบอทเอง (สำหรับนิสิต) — เวอร์ชัน Hermes Desktop
ให้นิสิตทุกคนสร้างบอท 3 ตัวนี้บน **Hermes Desktop** ด้วยตัวเองพร้อมๆ กัน
ไม่ต้องพิมพ์คำสั่งใดๆ ทั้งสิ้น — ทำผ่านเมาส์คลิกทั้งหมด

---

## ขั้นแรก: ดาวน์โหลดชุดไฟล์
1. เปิดหน้า GitHub / LMS ของรายวิชา
2. ดาวน์โหลด `workshop_kit` (ปุ่ม Code → Download ZIP หรือไฟล์แนบ)
3. แตกไฟล์เก็บไว้ที่ไหนก็ได้ในเครื่อง (เช่น โฟลเดอร์ Downloads)

ไฟล์ที่จะใช้มี:
- `bots/study-coach/SOUL.md`      ← บุคลิกพี่ติว
- `bots/data-analyst/SOUL.md`     ← บุคลิกนักวิเคราะห์
- `bots/content-creator/SOUL.md`  ← บุคลิกนักเขียน
- `data/sample_sales.csv`         ← ไฟล์ตัวอย่างให้นักวิเคราะห์
- `data/sample_lecture.pdf`       ← ไฟล์ตัวอย่างให้นิสิตลองสรุป

---

## ขั้นตอนที่ 1: สร้างบอท (ทำ 3 ครั้ง)
1. เปิด **Hermes Desktop**
2. ไปที่แท็บ **Bots** ด้านซ้าย/ล่าง
3. กดปุ่ม **"+" (สร้างบอทใหม่)**
4. กรอกข้อมูลในหน้าต่างที่เดียว:
   - **ชื่อ (Name):** `study-coach`
   - **คำอธิบาย (Description):** `พี่ติว: สรุปบทเรียน ทำข้อสอบฝึกหัด และตรวจให้`
   - **SOUL.md:** เปิดไฟล์ `bots/study-coach/SOUL.md` ด้วย Notepad → ก๊อปทั้งหมด → แปะลงช่อง SOUL.md
5. กด **สร้าง / Save**
6. ทำซ้ำสำหรับอีก 2 ตัว:
   - `data-analyst` / `นักวิเคราะห์: วิเคราะห์ CSV/Excel สรุปแนวโน้ม ทำกราฟ` / วางจาก `bots/data-analyst/SOUL.md`
   - `content-creator` / `นักเขียน: ทำสไลด์นำเสนอและภาพประกอบจากหัวข้อ` / วางจาก `bots/content-creator/SOUL.md`

> 💡 ชื่อบอทต้องตรงตัวสะกดเป๊ะ (study-coach / data-analyst / content-creator) เพื่อให้ขั้นตอนอื่นตรงกัน

---

## ขั้นตอนที่ 2: เตรียมไฟล์ตัวอย่าง
- **นักวิเคราะห์:** ก๊อปไฟล์ `data/sample_sales.csv` ไปไว้ในโฟลเดอร์ที่จำได้ (เช่น Desktop) เพื่อจะใส่ชื่อไฟล์ให้บอทอ่านทีหลัง
- **พี่ติว:** ก๊อปไฟล์ `data/sample_lecture.pdf` ไว้ด้วย เพื่อลองให้บอทสรุป

---

## ขั้นตอนที่ 3: ทดสอบว่าบอทมีชีวิต
1. ในแท็บ **Bots** คลิกเลือกบอท `study-coach`
2. พิมพ์ข้อความทักทาย:
   ```
   สวัสดี พี่ติว สรุปเรื่อง Neural Network ให้เข้าใจง่ายหน่อย
   ```
3. ถ้าบอทตอบกลับเป็นภาษาพี่รหัส → เยี่ยม เซ็ตต์สำเร็จ!
   (ถ้าระบบถามเลือก Model/Provider ตอนสร้าง ให้เลือก `tencent/hy3:free` / `nous`)

---

## ✨ ขั้นตอนที่ 4 (ตัวต่อขยาย): ให้บอททำงานตามเวลาเอง (Routine)
AI Agent ไม่ได้แค่รอให้เราพิมพ์ — ตั้งให้มันทำงานตามเวลาได้ด้วย
ลองตั้งให้ **พี่ติว** สรุปตารางเรียนทุกเช้า 08:00 น. เอง:

1. ในแท็บ **Bots** เลือกบอท `study-coach`
2. หาปุ่ม **Routine / Cron / ตั้งเวลา** (มักอยู่มุมขวาบนของแชทบอท)
3. กด **"+ เพิ่ม Routine"** แล้วกรอก:
   - **เวลา:** `ทุกวัน 08:00` (หรือเลือกจากตัวเลือกวัน/เวลาใน UI)
   - **คำสั่ง/ข้อความ:** `สรุปตารางเรียนวันนี้สั้นๆ 3 ข้อ`
   - **ส่งผลไปที่:** แชทของบอทนี้ (bot-chat)
4. กด **บันทึก**

💡 ให้นิสิตลองตั้งเวลาเป็น **"ทุก 2 นาที"** แล้วรอดูบอทส่งข้อความเข้าแชทเอง → เห็นภาพว่า AI ทำงานตามเวลาได้จริง

⚠️ หมายเหตุ: Routine จะรันได้ก็ต่อเมื่อ **Hermes เปิดอยู่ (Gateway ทำงาน)** และบอทอยู่บนเครื่องนั้น เครื่องห้องแล็บรีเซ็ตอาจหาย (เป็นเรื่องปกติ สำหรับสาธิต)

---

## ⚠️ ถ้าเจอปัญหา
- **ไม่มีแท็บ Bots** → Hermes อาจเป็นเวอร์ชั่นเก่า ร้องขอวิทยากรอัปเดต
- **สร้างบอทแล้วตอบว่า "ยังไม่มีโมเดล"** → ตอนสร้างบอท เลือก Model เป็น `tencent/hy3:free` และ Provider `nous`
- **บอทตอบไม่เหมือนพี่ติว** → ลองเปิดแก้ SOUL.md ดูว่าแปะครบหรือไม่ (เปิดบอท → ปุ่มแก้ไข → ดูช่อง SOUL.md)
- **Routine ไม่รัน** → ตรวจว่า Hermes เปิดค้างอยู่ และ Gateway เปิดอยู่ (ดูสถานะใน Settings)
- **ไม่แน่ใจก๊อป SOUL.md ถูกไหม** → พิมพ์ถามบอท `ใครเรียกพี่` ถ้าตอบ "พี่ติว" แสดงว่าถูก
